# 🏛️ FamStory

**FamStory** — это премиальная цифровая платформа для сохранения семейной истории. Сервис позволяет создавать эстетичные, кинематографичные страницы памяти ("цифровые мемориалы"), объединяя фотографии, биографии и хронологию жизни близких в единый интерактивный архив.

Проект разработан с акцентом на эмоциональный дизайн, безопасность данных и простоту использования.

---

## ✨ Ключевые Возможности

### 📖 Для Посетителей (Viewer)
*   **Кинематографичная подача:** Parallax-эффекты на обложке, плавные анимации появления контента.
*   **Галерея Bento Grid:** Умная сетка фотографий, адаптирующаяся под разные форматы.
*   **Хронология жизни (Timeline):** Визуализация ключевых событий от рождения до памятных дат.
*   **Семейное древо:** Интерактивная визуализация связей поколений.
*   **Адаптивность:** Полная поддержка мобильных устройств с удобным сенсорным управлением.

### ✍️ Для Создателей (Editor)
*   **Real-time Редактор:** Мгновенный предпросмотр изменений без перезагрузки.
*   **Управление архивами:** Создание множества семейных страниц (в зависимости от тарифа).
*   **Медиа-менеджмент:** Загрузка и организация фотографий (интеграция с Supabase Storage).
*   **Совместный доступ (RBAC):** Приглашение родственников с разными уровнями прав (Owner, Editor, Contributor, Viewer).
*   **Настройки приватности:** Управление доступом к архиву.

### 🔒 Безопасность и Правовые нормы
*   **Role-Based Security:** Доступ к изменению структуры древа защищен на уровне роутинга и API.
*   **GDPR / 152-ФЗ:** Встроенный механизм согласия с Cookie и обработкой персональных данных.
*   **Юридическая база:** Страницы "Политика конфиденциальности" и "Пользовательское соглашение".
*   **Защита данных:** RLS (Row Level Security) в базе данных Supabase гарантирует, что редактировать архив может только владелец или авторизованный редактор.

### 💰 Монетизация (Paywall)
Интегрированная система подписок с разделением прав доступа:
*   **Базовый (Бесплатно):** 1 архив, лимит до 30 фото.
*   **Хранитель (Guardian):** Безлимит архивов и фото, доступ к семейному древу, ежегодный бэкап.
*   **Наследие (Legacy):** Приоритетная поддержка, видео-истории.

*Система включает модальные окна апселла и имитацию платежного шлюза.*

### 📊 Аналитика
Система использует гибридный подход к сбору данных:
1.  **Google Analytics 4 / GTM:** Для внешнего маркетингового анализа.
2.  **Внутренняя аналитика (Supabase):** Собственная система сбора событий (`analytics_events`) для построения приватных дашбордов. Отслеживаются просмотры страниц, клики по CTA и регистрации.

### 🛡️ Админ-панель (Dashboard)
В проекте реализована скрытая админ-панель для просмотра внутренней статистики.

**Как попасть:**
1.  **Прямая ссылка:** Перейдите по адресу `/admin` (требуется авторизация).
2.  **Скрытый вход:** В футере сайта (внизу страницы), рядом с копирайтом, есть прозрачная ссылка "Admin". Наведите курсор, чтобы увидеть её.

**Функционал:**
*   Графики просмотров страниц за последние 7 дней.
*   Динамика новых регистраций.
*   Конверсия в целевые действия (клики по кнопкам).

---

## 🛠️ Технологический Стек

*   **Frontend:** Vue 3 (Composition API, `<script setup>`)
*   **Build Tool:** Vite 5 (с оптимизацией чанков и зависимостей)
*   **Architecture:** Feature-Sliced Design (FSD) / Modular Monolith
*   **State Management:** Pinia (модульная архитектура)
*   **Routing:** Vue Router 4 (с защитой маршрутов и восстановлением скролла)
*   **Styling:** Tailwind CSS 3 (кастомная конфигурация, Glassmorphism утилиты)
*   **Animations:** Motion One (`@vueuse/motion`) + CSS Transitions
*   **UX/UI:** Lucide Icons, Google Fonts (Playfair Display/Inter)
*   **Backend (BaaS):** Supabase (PostgreSQL, Auth, Storage, Edge Functions)

---

## 🚀 Запуск Проекта

### Предварительные требования
*   Node.js 18+
*   Аккаунт Supabase (для полной функциональности)

### Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/family-archive.git
    cd family-archive
    ```

2.  **Установите зависимости:**
    ```bash
    npm install
    ```

3.  **Настройка окружения:**
    Создайте файл `.env` в корне проекта (см. `.env.example`) и добавьте ключи Supabase и GA4:
    ```env
    VITE_SUPABASE_URL=your_supabase_url
    VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
    VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
    ```
    
    *Не забудьте применить SQL миграции из папки `supabase/migrations`!*

4.  **Запуск локально:**
    ```bash
    npm run dev
    ```
    Приложение будет доступно по адресу `http://localhost:5173`.

### Сборка для продакшена

```bash
npm run build
```
Результат будет в папке `dist`.

---

## 📂 Структура Проекта

Проект следует принципам модульной архитектуры (упрощенный FSD):

```
src/
├── assets/              # Статические ресурсы
├── composables/         # Глобальные хуки (useAnalytics, useSubscription)
├── layouts/             # Шаблоны страниц
├── modules/             # Бизнес-модули (Domain-Driven Design)
│   ├── access/          # RBAC: Управление правами и доступом
│   ├── admin/           # Админка и визуализация аналитики
│   ├── billing/         # Выбор тарифов и оплата (эмуляция)
│   └── family/          # Основной домен: управление архивами, людьми, хранение
│       ├── api/         # Репозитории (работа с БД)
│       ├── domain/      # Чистые TypeScript модели
│       ├── store/       # Pinia State
│       └── ui/          # Модульные компоненты
├── pages/               # Страницы приложения (точки входа роутера)
├── router/              # Конфигурация и Navigation Guards
├── shared/              # Общий код (Shared Layer)
│   ├── ui/              # Библиотека компонентов (Logo, BaseButton, BaseCard)
│   └── utils/           # Хелперы и словари
├── stores/              # Глобальные сторы (auth, subscription)
└── utils/               # Общие утилиты
```

---

## 💡 Особенности Реализации

*   **Dynamic Imports:** Маршруты загружаются лениво (Lazy Loading) для ускорения первой отрисовки.
*   **Mock Mode:** При переходе на `/smith-family` загружаются статические моковые данные для демонстрации возможностей без обращения к БД.
*   **Optimized Scroll:** Кастомная логика в роутере для сохранения позиции скролла при навигации "Назад".