# 🚀 Быстрый запуск в продакшн (Vercel)

Самый быстрый способ опубликовать твое приложение — использовать **Vercel** (бесплатно).

## Шаг 1: Подготовка
Убедись, что у тебя есть аккаунт на [vercel.com](https://vercel.com) (можно войти через GitHub).

## Шаг 2: Публикация через командную строку (Самый быстрый способ)

Не нужно создавать репозиторий на GitHub, если не хочешь. Можно задеплоить прямо из папки.

1. Установи Vercel CLI (в терминале редактора):
   ```bash
   npm i -g vercel
   ```

2. Запусти команду деплоя:
   ```bash
   npx vercel
   ```

3. Отвечай на вопросы в терминале:
   - `Set up and deploy?` → **y**
   - `Which scope?` → *(твое имя)*
   - `Link to existing project?` → **n**
   - `Project name?` → **family-archive** (или любое другое)
   - `In which directory?` → **./** (просто нажми Enter)
   - `Want to modify these settings?` → **n**

## Шаг 3: Настройка переменных окружения (Supabase)

Приложение *соберется*, но работать с базой данных не будет, пока мы не добавим ключи.

1. Когда команда завершится, ты увидишь ссылку `Production: https://family-archive-xyz.vercel.app`.
2. Зайди в панель управления Vercel (на сайте).
3. Выбери свой проект **family-archive**.
4. Перейди в **Settings** → **Environment Variables**.
5. Добавь те же переменные, что у тебя в локальном `.env` файле:
   - **Key**: `VITE_SUPABASE_URL`, **Value**: *(твой URL из Supabase)* → Add
   - **Key**: `VITE_SUPABASE_ANON_KEY`, **Value**: *(твой ключ из Supabase)* → Add

6. **Важно:** После добавления переменных нужно пересобрать проект.
   - Зайди в **Deployments**.
   - Нажми на три точки (⋮) у последнего деплоя → **Redeploy**.

## Готово! 🎉

Теперь твой сайт доступен по адресу вида:
`https://family-archive-xyz.vercel.app`

Эту ссылку можно отправлять родственникам.

### Ссылки на конкретные семьи:
- Главная: `https://...vercel.app/`
- Твой архив: `https://...vercel.app/semya-panasyuk`
