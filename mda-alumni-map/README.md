# Интерактивная карта выпускников МДА

Современное веб-приложение для укрепления связей между выпускниками Московской Духовной Академии. Проект позволяет выпускникам (духовенству и мирянам) отмечать свои места служения и работы на интерактивной карте.

## 🚀 Технологический стек

### Frontend
- **Vue 3 (Composition API)** + **Vite**
- **Tailwind CSS v4** (Modern UI/UX)
- **Pinia** (State Management)
- **Yandex Maps API v2.1**
- **Lucide Vue Next** (Иконки)

### Backend
- **Node.js** + **Express** + **TypeScript**
- **Prisma ORM**
- **SQLite** (База данных)
- **Zod** (Валидация данных)
- **JWT** (Авторизация)

---

## 🛠 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd mda-alumni-map
```

### 2. Настройка Бэкенда
```bash
cd server
npm install
npx prisma migrate dev --name init
npm run seed
npm run dev # Запуск на http://localhost:3000
```

### 3. Настройка Фронтенда
```bash
# В корневой папке проекта
npm install
npm run dev # Запуск на http://localhost:5173
```

---

## 🔐 Администрирование

Для доступа к панели модератора перейдите по адресу `/admin`.
- **Тестовый пароль:** `mda2026`
*(Примечание: в продакшене пароль должен быть заменен на хешированное значение в БД и управляться через переменные окружения).*

---

## 🌍 Работа в Production

При развертывании на реальный сервер:

1.  **Сборка Фронтенда:**
    Выполните `npm run build`. Содержимое папки `dist` должно раздаваться веб-сервером (Nginx/Apache).
2.  **API Proxy:**
    Nginx должен перенаправлять запросы с `/api` на порт бэкенда (по умолчанию 3000).
3.  **База данных:**
    В данном прототипе используется SQLite (`server/prisma/dev.db`). Для больших нагрузок рекомендуется переключить Prisma на PostgreSQL простым изменением в `schema.prisma`.
4.  **Геолокация:**
    Сайт требует HTTPS для корректной работы функции «Кто рядом?» в браузерах.
5.  **Яндекс Карты:**
    Замените подключение скрипта в `index.html` на версию с вашим API-ключом, полученным в кабинете разработчика Яндекса.

---

## 💎 Особенности реализации
- **Живой поиск:** Автодополнение адресов с приоритизацией по GPS пользователя.
- **Теологическая капча:** Защита от спам-ботов через контрольные вопросы по профилю обучения.
- **Mobile First:** Интерфейс полностью адаптирован под жесты (swipe-to-close) и малые экраны.
- **SEO Ready:** Динамические мета-теги для каждого профиля выпускника.