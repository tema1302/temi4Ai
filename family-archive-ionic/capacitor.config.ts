import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.famstory.app',
  appName: 'FamStory',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
